# ==================================
# DEVTREE BACKEND - VARIABLES DE ENTORNO
# ==================================

# ----------------
# Database
# ----------------
# MongoDB Connection String
# Obtén esto de MongoDB Atlas: https://www.mongodb.com/cloud/atlas
# IMPORTANTE: Usa MONGO_URI (no DATABASE_URL)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/devtree

# ----------------
# Server
# ----------------
PORT=4000
NODE_ENV=development

# ----------------
# CORS
# ----------------
# URL del frontend (sin / al final)
# Desarrollo: http://localhost:5173
# Producción: https://tu-app.netlify.app o https://tu-app.vercel.app
FRONTEND_URL=http://localhost:5173

# ----------------
# JWT (JSON Web Token)
# ----------------
# Genera un secret seguro y largo
# Puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu-secret-super-seguro-cambia-esto-en-produccion

# ----------------
# Cloudinary (Almacenamiento de imágenes)
# ----------------
# Obtén estas credenciales de: https://cloudinary.com
CLOUDINARY_NAME=tu-cloud-name
CLOUDINARY_API_KEY=tu-api-key
CLOUDINARY_API_SECRET=tu-api-secret

# ==================================
# NOTAS IMPORTANTES
# ==================================
# 1. NUNCA subas el archivo .env al repositorio
# 2. Copia este archivo como .env y completa con tus valores reales
# 3. En producción (Render), configura estas variables en el dashboard
# 4. Para generar un JWT_SECRET seguro ejecuta:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# VARIABLES PARA PRODUCCIÓN EN RENDER:
# - MONGO_URI: Tu string de conexión de MongoDB Atlas
# - NODE_ENV: production
# - FRONTEND_URL: https://tu-app-frontend.netlify.app (o vercel.app)
# - JWT_SECRET: Un string aleatorio seguro
# - CLOUDINARY_NAME: Tu cloud name de Cloudinary
# - CLOUDINARY_API_KEY: Tu API key de Cloudinary
# - CLOUDINARY_API_SECRET: Tu API secret de Cloudinary
